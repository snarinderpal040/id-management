spring:
  application:
    name: identity-management-app

  security:
    oauth2:
      client:
        registration:
#          google:
#            client-id:
#            client-secret:
#          facebook:
#            client-id:
#            client-secret:

  jpa:
    database: oracle
    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: none
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    username: 
    password: 
    url: jdbc:oracle:thin:@//oracledb:1521/ORCLCDB

server:
  port: 8080

